package enshud.ast;

/** 代入左辺の配列要素 */
public final class ArrayAccess implements LValue {
    private final String name;
    private final Expr index;
    private final int line;

    // binding
    private int baseOffset = -1;
    private int lower = 0;
    private BuiltinType elemType = null;

    public ArrayAccess(String name, Expr index, int line) {
        this.name = name;
        this.index = index;
        this.line = line;
    }

    public String name() { return name; }
    public Expr index() { return index; }
    public int line() { return line; }

    public void bindArray(int baseOffset, int lower, BuiltinType elemType) {
        this.baseOffset = baseOffset;
        this.lower = lower;
        this.elemType = elemType;
    }

    public int baseOffset() { return baseOffset; }
    public int lower() { return lower; }
    public BuiltinType elemType() { return elemType; }

    @Override
    public <R> R accept(AstVisitor<R> v) {
        return v.visitArrayAccess(this);
    }
}
